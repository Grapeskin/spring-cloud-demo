server:
  port: 7005

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/grapeskin/spring-cloud-demo
          search-paths: respo
          username:
          password:
      label: master
